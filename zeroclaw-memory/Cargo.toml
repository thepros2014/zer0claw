[package]
name = "zeroclaw-memory"
version = "0.1.0"
edition = "2021"
description = "Durable memory backend for ZeroClaw using SQLite"

[dependencies]
zeroclaw-api = { path = "../zeroclaw-api" }
serde_json = "1.0"
