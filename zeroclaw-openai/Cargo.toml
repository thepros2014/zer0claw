[package]
name = "zeroclaw-openai"
version = "0.1.0"
edition = "2021"
description = "Real OpenAI LLM Provider for the ZeroClaw AI Framework"

[dependencies]
zeroclaw-api = { path = "../zeroclaw-api" }
serde_json = "1.0"
reqwest = { version = "0.12", features = ["blocking", "json"] }
