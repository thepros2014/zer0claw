[package]
name = "zeroclaw-provider"
version = "0.1.0"
edition = "2021"
description = "Mock LLM Provider for the ZeroClaw AI Framework"

[dependencies]
zeroclaw-api = { path = "../zeroclaw-api" }
serde_json = "1.0"
